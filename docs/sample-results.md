# Sample SKILLCHECK Report

This report was generated by running:

```bash
python -m skillcheck.cli lint examples/brand-voice-editor --output-dir docs/artifacts
python -m skillcheck.cli probe examples/brand-voice-editor --output-dir docs/artifacts
python -m skillcheck.cli lint examples/risky-net-egress --output-dir docs/artifacts
python -m skillcheck.cli probe examples/risky-net-egress --exec --output-dir docs/artifacts
python -m skillcheck.cli report . --artifacts docs/artifacts --fail-on-failures
```

## Summary

| Total Skills | Pass | Fail |
| --- | --- | --- |
| 2 | 1 | 1 |

## Detailed Findings

| Skill | Version | Lint Violations | Probe Issues | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| brand-voice-editor | - | 0 | 0 | ✅ PASS | Clean Skill referencing a static brand guide. |
| risky-net-egress | - | 3 | 5 | ❌ FAIL | Attempts `curl http://example.com`, `urllib.request.urlopen('https://example.com')`, and writes `../outside.txt`. Sandbox execution blocks both egress and write. |

### Example Lint Findings (excerpt)

```json
{
  "code": "forbidden_pattern_2",
  "path": "scripts/unsafe_shell.sh",
  "message": "Raw network call via curl (HTTP/HTTPS) is forbidden without allowlist",
  "severity": "error"
}
```

### Example Probe Findings (excerpt)

```json
{
  "code": "EGRESS_SANDBOX",
  "message": "scripts/do_egress.py: urlopen to example.com blocked"
}
```

### Artifacts Produced

- `docs/artifacts/results.md` — human-readable summary (same table as above).
- `docs/artifacts/results.json` — machine-readable summary for CI gating.
- `docs/artifacts/` — committed sample outputs for documentation and reviews.

Use this sample as a reference when reviewing real runs.
